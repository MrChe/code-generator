import {{ camelCase name }} from 'reducers/{{ camelCase name }}';

describe('{{ camelCase name }}', () => {
  it('returns the initial state', () => {
    expect({{ camelCase name }}(undefined, {})).toEqual({});
  });
});
