/*
 *
 * {{ constantCase name }} reducer
 *
 */
import {{ constantCase name }} from 'types/{{ camelCase name }}';

const initialState = {};

export default (state = initialState, { type, payload }) => {
  switch (action.type) {
    case {{ constantCase name }}.DEFAULT_ACTION:
      return state;
    default:
      return state;
  }
}
