/*
 *
 * {{ constantCase name }} actions
 *
 */

import action from 'utils/createAction';
import {{ constantCase name }} from 'types/{{ camelCase name }}';

export default {
  default: (n) => action({{ constantCase name }}.DEFAULT, n),
};

